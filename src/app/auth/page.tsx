import { AuthScreen } from "@/features/auth/components/auth-screen";

const AuthPage = () => {
  return (
    <div className="bg-brand min-h-screen flex flex-col items-center justify-center">
      <AuthScreen />
    </div>
  );
};

export default AuthPage;
